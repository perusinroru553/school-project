#include <stdio.h>

int main() {
    int x = 5;
    if (x > 0) {
        printf("Positive number\n");
    } else if (x == 0) {
        printf("Zero\n");
    } else {
        printf("Negative number\n");
    }
    return 0;
}
